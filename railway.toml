[build]
builder = "nixpacks"
# Force le dossier racine pour le build
watchPatterns = ["backend/**"]

[deploy]
startCommand = "cd backend && npm start"
restartPolicyType = "ON_FAILURE"

[service]
# Indique a Railway ou se trouve le code
rootDirectory = "backend"
